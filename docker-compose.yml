services:
  telegram-bot:
    build: .
    container_name: telgram-sentinel-bot
    environment:
      BOT_TOKEN: ${BOT_TOKEN}
      ADMIN_ID: ${ADMIN_ID}
      GROUP_ID: ${GROUP_ID}
      CHECK_HOUR: "20"
